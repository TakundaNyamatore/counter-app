import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    padding: 8,
    backgroundColor: "grey",
    borderRadius: 8,
    margin: 4,
  },
  btnText: {
    fontSize: 16,
    color: "white",
  },
});
